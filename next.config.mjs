/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos'], // Add your external image domains here
  },
  darkMode: 'class', // Tailwind CSS dark mode class configuration
};

export default nextConfig;
